# npm configuration for the frontend package
#
# legacy-peer-deps is required due to version mismatches between:
# - React 19.1.0 (app dependency)
# - jest-expo 52 (dev dependency)
# - @testing-library packages (dev dependencies)
#
# These packages have peer dependency conflicts that don't affect runtime behavior.
# This setting allows npm to install despite peer dependency conflicts.
legacy-peer-deps=true
